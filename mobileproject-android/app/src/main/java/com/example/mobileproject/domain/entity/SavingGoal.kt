package com.example.mobileproject.domain.entity

/**
 * File này đã không còn được sử dụng (deprecated).
 * Tất cả các entity liên quan đến Goal (SavingGoal, FutureGoal, GoalTask)
 * và các enum (GoalStatus, GoalType) đã được chuyển sang file [Goal.kt]
 * để hỗ trợ xử lý dữ liệu đa hình (polymorphic) với sealed class.
 *
 * @see Goal
 * @see SavingGoal
 * @see FutureGoal
 */
